{
  "name": "mediasoup-vue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup:server": "cd server && yarn",
    "setup:client": "cd client && yarn",
    "setup": "yarn setup:server && yarn setup:client",
    "dev:server": "cd server && yarn dev",
    "dev:client": "cd client && yarn dev",
    "dev:todo:1": "concurrently 'yarn dev:server' && 'yarn dev:client'",
    "dev:todo:2": "yarn dev:server; yarn dev:client"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
